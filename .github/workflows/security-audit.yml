name: Dependency security audit

on:
  push:
    branches: ["main"]
    paths:
      - "release/web-demo/requirements.txt"
      - ".github/workflows/security-audit.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "release/web-demo/requirements.txt"
  schedule:
    # Weekly, even if nothing changed, so newly-disclosed CVEs are caught.
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  pip-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Install pip-audit
        run: python -m pip install --upgrade pip pip-audit

      - name: Audit pinned dependencies for known CVEs
        run: python -m pip-audit -r release/web-demo/requirements.txt
